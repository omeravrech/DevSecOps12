import hangingman
import random

choosen_index = 0 #random.randint(0, len(hangingman.sentences_list))
indexs_that_choosed = []
while True:

    # sentence = hangingman.hide_chars(hangingman.sentences_list[choosen_index])
    # print(f"{choosen_index}. {sentence}")

